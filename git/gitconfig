[alias]
  st = status
  co = checkout
  diff = diff --word-diff
  lg = log --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset' --abbrev-commit --date=relative
[core]
  editor = nvim -f
  pager = less -FRSX
[pager]
  log = less
  show = less
  diff = less
[color]
  ui = auto
  diff = auto
  status = auto
  branch = auto
  interactive = auto
[merge]
  tool = nvim
[mergetool "nvim"]
  cmd = nvim -d $LOCAL $MERGED $REMOTE
  keepBackup = false
[mergetool]
  keepBackup = false
[pull]
  rebase = true
[push]
  default = simple
[url "git@gitlab.com:"]
  insteadOf = https://gitlab.com/
[url "git@github.com:"]
  insteadOf = https://github.com/

# Include local/private config (name, email, signing key)
[include]
  path = gitconfig.local
